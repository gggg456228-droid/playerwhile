name: playerwhile
description: Cross-platform floating GitHub character updater.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  path: ^1.9.1
  path_provider: ^2.1.5
  window_manager: ^0.5.1

flutter:
  uses-material-design: true
